#include "lists.h"
/**
 * is_palindrome - s
 * @head: s
 * Return: s
 */
int is_palindrome(listint_t **head)
{
	int i = 0, j = 0, k = 0;
	listint_t **head_two = *head, **home = *head;

	while (*head_two != NULL)
	{
		*head_two->next;
		i++;
	}
	*head_two = *head;
	for (j = 0; j < i - k; k++)
	{

